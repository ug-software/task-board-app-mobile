/** @format */

import { styled } from "@/theme";
/*
export const whapperLoginPage = styled({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  height: "100%",
  width: "100%",
});*/
/*
export const containerLoginPage = styled({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  height: "100%",
  width: "100%",
  padding: 20,
});

export const imageInitial = styled({
  width: "100%",
  objectFit: "contain",
  marginBottom: 50,
});

export const backgroundInitial = styled({
  position: "absolute",
  objectFit: "cover",
  width: "100%",
  height: "100%",
});

export const textTitle = styled({
  fontSize: 26,
  fontWeight: 800,
  marginBottom: 10,
});

export const textDescription = styled({
  fontSize: 14,
  fontWeight: 400,
  textAlign: "center",
  marginBottom: 10,
});
*/
