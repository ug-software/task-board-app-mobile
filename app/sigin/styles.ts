/** @format */

import styled from "@/theme/styled";
import { View } from "react-native";
/*
export const WhapperSiginView = styled(View)(() => ({
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  height: "100%",
  width: "100%",
}));

export const HeaderSiginView = styled(View)(() => ({
  display: "flex",
  flexDirection: "row",
  alignItems: "center",
  width: "100%",
  marginTop: 45,
  padding: 20,
}));

export const TextSiginHeader = styled(Text)(() => ({
  width: "75%",
  textAlign: "center",
  fontSize: 21,
  fontWeight: 700,
}));

export const ContainerSiginView = styled(View)(() => ({
  width: "100%",
  marginBottom: 55,
  padding: 20,
}));*/
