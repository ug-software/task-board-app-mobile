/** @format */

import styled from "@/theme/styled";
import { View } from "react-native";
/*
export const WhapperLoginView = styled(View)(() => ({
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  height: "100%",
  width: "100%",
}));

export const ContainerLoginView = styled(View)(() => ({
  width: "100%",
  marginBottom: 55,
  padding: 20,
}));

export const HeaderLoginView = styled(View)(() => ({
  width: "100%",
  marginTop: 45,
  padding: 20,
}));*/
