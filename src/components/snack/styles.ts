/** @format */

import { styled } from "@/src/theme";

export default styled()({
    whapperSnackProvider: {
        zIndex: 999,
        position: "absolute",
        top: 40,
        paddingHorizontal: 15
    }
});
