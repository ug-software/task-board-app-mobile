/** @format */

import { styled } from "@/theme";

export default styled(({ theme }) => ({
  whapperTollbar: {
    position: "relative",
    height: 45,
    backgroundColor: theme.pallet.primary.background,
  },
}));
